language: python

python:
  - "3.6"      # current default Python on Travis CI
  - "3.7"
  - "3.8"

service:
  - mysql

before_install:
  - mysql -e 'CREATE DATABASE IF NOT EXISTS musicavis_test;'

install:
  - pip install -r requirements.txt

script:
  - python manage.py test && flake8
